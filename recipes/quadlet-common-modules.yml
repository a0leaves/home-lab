---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json

modules:
  - type: files
    files:
      - source: linger-system
        destination: /
  - type: systemd
    user:
      enabled:
        - podman-auto-update.timer
